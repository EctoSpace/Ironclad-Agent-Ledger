# PostgreSQL connection URL for the Ironclad Agent Ledger.
# The agent state (event log and snapshots) lives here.
DATABASE_URL=postgres://user:password@localhost:5432/ironclad_ledger

# Optional: run cognitive loop with --agent. Ollama must be running on OLLAMA_BASE_URL.
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=mistral
# Comma-separated domains allowed for Tripwire http_get (e.g. api.example.com).
# AGENT_ALLOWED_DOMAINS=
# Max steps per run (default: no limit).
# AGENT_MAX_STEPS=10
